#name: Simple class.
# --
function ${1:Bacon}($2) {
  if ( !(this instanceof arguments.callee) )
      return new arguments.callee(arguments);

  // this.name = name;
  // this.doThing = function () { ... }

  $0
}